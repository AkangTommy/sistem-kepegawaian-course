title: sistem kepegawaian
description: cari sistem
template:
  repo: sistem-kepegawaian-template
  name: sistem-kepegawaian
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
